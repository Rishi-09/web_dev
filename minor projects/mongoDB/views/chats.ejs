<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>chats</title>
  </head>
  <body>
    <h1 style="text-align: center">Chat Cards</h1>
    <div
      class="cards"
      style="
        display: flex;
        flex-wrap: wrap;
        width: 100vw;
        height: 100vh;
        background: linear-gradient(125deg, #1135ac, #3c5fd3, #19bdaf);
      "
    >
      <% for( chat of chats ) { %>
      <div
        class="chat"
        style="
          border: 2px solid rgba(255, 255, 255, 0.3);
          border-radius: 12px;
          width: 350px;
          height: 500px;
          background-color: #435dcf;
          backdrop-filter: blur(12px);
          margin: 10px;
          padding: 10px;
        "
      >
        <p><%=chat.from%>: <%=chat.msg%></p>
        <p>to :<%=chat.to%></p>
        <p class="time" style="position: relative; bottom: -380px">
          <%=chat.created_at%>
        </p>
      </div>
      <% } %>
    </div>
  </body>
</html>
