<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>chats</title>
  </head>
  <body>
    <h1 style="text-align: center">Chat Cards</h1>
    <form action="/chats/new" method="GET" >
      <button style="padding: 5px; margin: 10px; border-radius: 10px; background-color: antiquewhite; color: black;" >New Chat</button>
    </form>
    <div
      class="cards"
      style="
        display: flex;
        flex-wrap: wrap;
        width: 100vw;
        height: 100vh;
        background: linear-gradient(125deg, #1135ac, #3c5fd3, #19bdaf);
      "
    >
      <% for( chat of chats ) { %>
      <div
        class="chat"
        style="
        
          border: 2px solid rgba(255, 255, 255, 0.3);
          border-radius: 12px;
          width: 350px;
          height: 200px;
          background-color: #435dcf;
          backdrop-filter: blur(12px);
          margin: 10px;
          padding: 10px;
        "
      >
        <p><%=chat.from%>: <%=chat.msg%></p>
        <p>to :<%=chat.to%></p>
        <p><%=chat.created_at.toString().split(" ")[4]%></p>
        <p><%=chat.created_at.toString().split(" ").slice(0,4).join(' ')%></p>
        <form action="/chats/<%=chat._id%>/edit" method="GET" > <button>Edit</button> </form>
        <form action="/chats/<%=chat._id%>?_method=DELETE" method="POST" > <button>Delete</button> </form>
        <form action="/chats/<%=chat.id%>" method="GET" ><button>Show</button></form>
      </div>
      <% } %>
    </div>
  </body>
</html>
